/**
 * Represents the game boy CPU.
 * <br>
 * Will hold ALL the instructions (bit manipulations), and registers from the unit.
 */
public class CPU {

    /**
     * Constructs..
     * TODO: figure out what we need to set up as we construct
     */
    public CPU() {
        // pass memory instance here (so they link)
    }
}
